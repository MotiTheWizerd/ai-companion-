/**
 * Controls the display of user messages, specifically handling memory blocks.
 */
export class UserMessageController {
  constructor() {
    console.log('[UserMessageController] DISABLED to prevent crashes');
  }

  init() {
    console.log('[UserMessageController] init called but disabled');
  }

  startObserver() { }
  scanForMemoryBlocks(rootNode) { }
  processMemoryBlock(element) { }
  escapeHtml(text) { }
}
